cmake_minimum_required(VERSION 3.25)
file(READ "${CMAKE_CURRENT_SOURCE_DIR}/../../package.json" ceres_package_json)
string(JSON CERES_VIEWER_VERSION GET "${ceres_package_json}" version)
string(REGEX MATCH "^[0-9]+\\.[0-9]+\\.[0-9]+" ceres_project_version "${CERES_VIEWER_VERSION}")
project(ceres_viewer VERSION "${ceres_project_version}" LANGUAGES C CXX)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
option(CERES_BUILD_APP "Build the native NVIDIA viewer" ON)
option(CERES_BUILD_TESTS "Build deterministic acceptance tests" ON)
include(cmake/VideoBackend.cmake)
if(NOT DEFINED CMAKE_CUDA_ARCHITECTURES)
  if(CERES_SELECTED_VIDEO_BACKEND STREQUAL "JETSON")
    set(CMAKE_CUDA_ARCHITECTURES "87" CACHE STRING "CUDA device architectures")
  elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "^(aarch64|arm64|ARM64)$")
    # Include PTX for newer GPUs without requiring a Blackwell-capable toolkit.
    # The release workflow pins its wider set of native GPU targets separately.
    set(CMAKE_CUDA_ARCHITECTURES "75;80;86;89;90" CACHE STRING "CUDA device architectures")
  elseif(NOT WIN32)
    set(CMAKE_CUDA_ARCHITECTURES "75;86;89;120" CACHE STRING "CUDA device architectures")
  else()
    set(CMAKE_CUDA_ARCHITECTURES "75;86;89" CACHE STRING "CUDA device architectures")
  endif()
endif()
include(FetchContent)
set(FETCHCONTENT_QUIET OFF)
set(BUILD_SHARED_LIBS OFF CACHE BOOL "" FORCE)
find_package(Threads REQUIRED)
FetchContent_Declare(json URL https://github.com/nlohmann/json/archive/9cca280a4d0ccf0c08f47a99aa71d1b0e52f8d03.tar.gz URL_HASH SHA256=0dbc5e40a01ff142e7e68c03e85247a4dcede2f592d12d3677dee3664d17975a DOWNLOAD_EXTRACT_TIMESTAMP TRUE)
FetchContent_MakeAvailable(json)
FetchContent_Declare(mcap URL https://github.com/foxglove/mcap/archive/a7eeff1383cc95a29dd9c5cac94104d277b5414e.tar.gz URL_HASH SHA256=2456386bdd606bdefcdfe0255f169f82d5bcbd2d9539a64e44e5917c7b1994e0 DOWNLOAD_EXTRACT_TIMESTAMP TRUE)
FetchContent_GetProperties(mcap)
if(NOT mcap_POPULATED)
  FetchContent_Populate(mcap)
endif()
add_library(ceres_core STATIC src/protocol.cpp src/session.cpp src/calibration.cpp src/hand_trails.cpp src/hand_mask.cpp src/stereo.cpp src/depth.cpp src/task_specification.cpp src/replay_task.cpp src/spatial_map.cpp)
target_include_directories(ceres_core PUBLIC include "${mcap_SOURCE_DIR}/cpp/mcap/include")
target_link_libraries(ceres_core PUBLIC nlohmann_json::nlohmann_json Threads::Threads)
target_compile_definitions(ceres_core PUBLIC MCAP_COMPRESSION_NO_LZ4 MCAP_COMPRESSION_NO_ZSTD)
if(MSVC)
  target_compile_options(ceres_core PRIVATE /W4 /permissive-)
else()
  target_compile_options(ceres_core PRIVATE -Wall -Wextra -Wpedantic)
endif()
add_library(ceres_export_job STATIC src/export_job.cpp)
target_link_libraries(ceres_export_job PUBLIC ceres_core)
if(CERES_BUILD_TESTS)
  enable_testing()
  foreach(test protocol session geometry video_queue hand_trails hand_mask stereo stereo_pairing accordion_motion stereo_cadence depth tracking_visibility task_specification replay_task recording_bar spatial_map)
    add_executable(test_${test} tests/test_${test}.cpp)
    target_link_libraries(test_${test} PRIVATE ceres_core)
    add_test(NAME ${test} COMMAND test_${test})
  endforeach()
  add_executable(test_export_job tests/test_export_job.cpp)
  target_link_libraries(test_export_job PRIVATE ceres_export_job)
  add_test(NAME export_job COMMAND test_export_job)
endif()
if(CERES_BUILD_APP)
  include(cmake/Dependencies.cmake)
  add_library(ceres_task_source STATIC src/task_source.cpp)
  target_link_libraries(ceres_task_source PUBLIC ceres_core PRIVATE CURL::libcurl)
  add_library(ceres_hugging_face STATIC src/hugging_face.cpp)
  target_link_libraries(ceres_hugging_face PUBLIC ceres_core PRIVATE ceres_export_job CURL::libcurl mbedcrypto)
  target_compile_definitions(ceres_hugging_face PRIVATE NOMINMAX)
  if(WIN32)
    target_link_libraries(ceres_hugging_face PRIVATE crypt32 shell32)
  endif()
  if(CERES_BUILD_TESTS)
    add_executable(ceres-hf-tool tests/hugging_face_cli.cpp)
    target_link_libraries(ceres-hf-tool PRIVATE ceres_hugging_face)
    add_executable(test_hugging_face tests/test_hugging_face.cpp)
    target_link_libraries(test_hugging_face PRIVATE ceres_hugging_face mbedcrypto)
    add_test(NAME hugging_face COMMAND test_hugging_face)
    set_tests_properties(hugging_face PROPERTIES TIMEOUT 60)
    add_executable(test_task_source tests/test_task_source.cpp)
    target_link_libraries(test_task_source PRIVATE ceres_task_source)
    if(WIN32)
      target_link_libraries(test_task_source PRIVATE ws2_32)
    endif()
    add_test(NAME task_source COMMAND test_task_source)
    set_tests_properties(task_source PROPERTIES TIMEOUT 45)
    add_executable(test_bridge tests/test_bridge.cpp src/bridge.cpp)
    target_link_libraries(test_bridge PRIVATE ceres_core datachannel-static CURL::libcurl)
    target_compile_definitions(test_bridge PRIVATE NOMINMAX)
    if(WIN32)
      target_link_libraries(test_bridge PRIVATE bcrypt crypt32 advapi32 ws2_32)
    endif()
    add_test(NAME bridge COMMAND test_bridge)
    set_tests_properties(bridge PROPERTIES TIMEOUT 60)
  endif()
  enable_language(CUDA)
  find_package(CUDAToolkit REQUIRED)
  ceres_add_video_library()
  if(CERES_BUILD_TESTS)
    add_executable(test_mesh tests/test_mesh.cpp src/mesh.cpp src/mano.cpp)
    target_link_libraries(test_mesh PRIVATE ceres_core glm::glm)
    target_compile_definitions(test_mesh PRIVATE GLM_ENABLE_EXPERIMENTAL GLM_FORCE_CTOR_INIT
      CERES_TEST_ASSET_DIRECTORY="${CMAKE_CURRENT_SOURCE_DIR}/assets")
    add_test(NAME mesh COMMAND test_mesh)
    add_executable(test_static_model tests/test_static_model.cpp src/static_model.cpp)
    target_link_libraries(test_static_model PRIVATE ceres_core glm::glm)
    target_compile_definitions(test_static_model PRIVATE GLM_ENABLE_EXPERIMENTAL GLM_FORCE_CTOR_INIT)
    add_test(NAME static_model COMMAND test_static_model)
    add_executable(test_spatial_map_render tests/test_spatial_map_render.cpp)
    target_include_directories(test_spatial_map_render PRIVATE include)
    target_link_libraries(test_spatial_map_render PRIVATE glfw glad_gl_core_45 glm::glm)
    target_compile_definitions(test_spatial_map_render PRIVATE GLFW_INCLUDE_NONE NOMINMAX GLM_FORCE_CTOR_INIT)
    add_test(NAME spatial_map_render COMMAND test_spatial_map_render)
    set_tests_properties(spatial_map_render PROPERTIES LABELS "gpu" TIMEOUT 60)
    add_executable(test_spatial_map_lifecycle tests/test_spatial_map_lifecycle.cpp
      src/renderer.cpp src/mesh.cpp src/mano.cpp src/static_model.cpp
      src/image.cu src/stereo.cu src/voxel.cu src/depth.cu)
    target_include_directories(test_spatial_map_lifecycle PRIVATE "${nvcodec_SOURCE_DIR}/include/ffnvcodec")
    target_link_libraries(test_spatial_map_lifecycle PRIVATE ceres_video glfw glad_gl_core_45 glm::glm
      CUDA::cuda_driver CUDA::cudart ${CMAKE_DL_LIBS})
    target_compile_definitions(test_spatial_map_lifecycle PRIVATE GLFW_INCLUDE_NONE NOMINMAX
      GLM_ENABLE_EXPERIMENTAL GLM_FORCE_CTOR_INIT CERES_TEST_ASSET_DIRECTORY="${CMAKE_CURRENT_SOURCE_DIR}/assets")
    set_target_properties(test_spatial_map_lifecycle PROPERTIES CUDA_STANDARD 17)
    if(MSVC)
      target_compile_options(test_spatial_map_lifecycle PRIVATE $<$<COMPILE_LANGUAGE:CUDA>:-allow-unsupported-compiler>)
      target_compile_definitions(test_spatial_map_lifecycle PRIVATE _ALLOW_COMPILER_AND_STL_VERSION_MISMATCH)
    endif()
    add_test(NAME spatial_map_lifecycle COMMAND test_spatial_map_lifecycle)
    set_tests_properties(spatial_map_lifecycle PROPERTIES LABELS "gpu" TIMEOUT 60)
    add_executable(test_image tests/test_image.cu src/image.cu)
    target_include_directories(test_image PRIVATE include)
    target_link_libraries(test_image PRIVATE CUDA::cudart)
    set_target_properties(test_image PROPERTIES CUDA_STANDARD 17)
    if(MSVC)
      target_compile_options(test_image PRIVATE $<$<COMPILE_LANGUAGE:CUDA>:-allow-unsupported-compiler>)
      target_compile_definitions(test_image PRIVATE _ALLOW_COMPILER_AND_STL_VERSION_MISMATCH)
    endif()
    add_test(NAME image_cuda COMMAND test_image)
    set_tests_properties(image_cuda PROPERTIES LABELS "gpu")
    add_executable(test_stereo_cuda tests/test_stereo_cuda.cu src/stereo.cu)
    target_include_directories(test_stereo_cuda PRIVATE include)
    target_link_libraries(test_stereo_cuda PRIVATE ceres_core CUDA::cudart)
    set_target_properties(test_stereo_cuda PROPERTIES CUDA_STANDARD 17)
    if(MSVC)
      target_compile_options(test_stereo_cuda PRIVATE $<$<COMPILE_LANGUAGE:CUDA>:-allow-unsupported-compiler>)
      target_compile_definitions(test_stereo_cuda PRIVATE _ALLOW_COMPILER_AND_STL_VERSION_MISMATCH)
    endif()
    add_test(NAME stereo_cuda COMMAND test_stereo_cuda)
    set_tests_properties(stereo_cuda PROPERTIES LABELS "gpu" TIMEOUT 60)
    add_executable(test_voxel_cuda tests/test_voxel_cuda.cu src/voxel.cu)
    target_include_directories(test_voxel_cuda PRIVATE include)
    target_link_libraries(test_voxel_cuda PRIVATE CUDA::cudart)
    set_target_properties(test_voxel_cuda PROPERTIES CUDA_STANDARD 17)
    if(MSVC)
      target_compile_options(test_voxel_cuda PRIVATE $<$<COMPILE_LANGUAGE:CUDA>:-allow-unsupported-compiler>)
      target_compile_definitions(test_voxel_cuda PRIVATE _ALLOW_COMPILER_AND_STL_VERSION_MISMATCH)
    endif()
    add_test(NAME voxel_cuda COMMAND test_voxel_cuda)
    set_tests_properties(voxel_cuda PROPERTIES LABELS "gpu" TIMEOUT 60)
    add_executable(test_depth_cuda tests/test_depth_cuda.cu src/depth.cu)
    target_include_directories(test_depth_cuda PRIVATE include)
    target_link_libraries(test_depth_cuda PRIVATE CUDA::cudart)
    set_target_properties(test_depth_cuda PROPERTIES CUDA_STANDARD 17)
    if(MSVC)
      target_compile_options(test_depth_cuda PRIVATE $<$<COMPILE_LANGUAGE:CUDA>:-allow-unsupported-compiler>)
      target_compile_definitions(test_depth_cuda PRIVATE _ALLOW_COMPILER_AND_STL_VERSION_MISMATCH)
    endif()
    add_test(NAME depth_cuda COMMAND test_depth_cuda)
    set_tests_properties(depth_cuda PROPERTIES LABELS "gpu" TIMEOUT 60)
    add_executable(test_nvdec tests/test_nvdec.cpp)
    add_executable(test_video_backend tests/test_video_backend.cpp src/nvdec.cpp)
    target_link_libraries(test_video_backend PRIVATE ceres_core CUDA::cuda_driver)
    add_test(NAME video_backend COMMAND test_video_backend)
    set_tests_properties(video_backend PROPERTIES LABELS "gpu" TIMEOUT 15)
    target_include_directories(test_nvdec PRIVATE "${nvcodec_SOURCE_DIR}/include/ffnvcodec")
    target_compile_definitions(test_nvdec PRIVATE NOMINMAX)
    target_link_libraries(test_nvdec PRIVATE ceres_video)
    add_test(NAME nvdec COMMAND test_nvdec --run-gpu
      "${CMAKE_CURRENT_SOURCE_DIR}/tests/fixtures/nvdec"
      "${CMAKE_CURRENT_BINARY_DIR}/nvdec-check.json")
    set_tests_properties(nvdec PROPERTIES LABELS "gpu" TIMEOUT 60)
  endif()
  add_executable(ceres-viewer src/main.cpp src/app.cpp src/ui.cpp src/renderer.cpp src/mesh.cpp src/mano.cpp src/static_model.cpp src/image.cu src/stereo.cu src/voxel.cu src/depth.cu src/bridge.cpp)
  include(cmake/NativeExporter.cmake)
  ceres_add_native_exporter(ceres-viewer)
  set_target_properties(ceres-viewer PROPERTIES CUDA_STANDARD 17)
  target_compile_definitions(ceres-viewer PRIVATE CERES_VIEWER_VERSION="${CERES_VIEWER_VERSION}")
  target_compile_definitions(ceres-viewer PRIVATE GLFW_INCLUDE_NONE NOMINMAX GLM_FORCE_CTOR_INIT GLM_ENABLE_EXPERIMENTAL)
  target_include_directories(ceres-viewer PRIVATE "${nvcodec_SOURCE_DIR}/include/ffnvcodec")
  target_link_libraries(ceres-viewer PRIVATE ceres_video ceres_export_job ceres_task_source ceres_hugging_face imgui glfw glad_gl_core_45 glm::glm datachannel-static CURL::libcurl qrcodegen CUDA::cuda_driver CUDA::cudart ${CMAKE_DL_LIBS})
  if(MSVC)
    set_target_properties(ceres-viewer PROPERTIES WIN32_EXECUTABLE TRUE)
    target_link_options(ceres-viewer PRIVATE /ENTRY:mainCRTStartup)
    target_compile_options(ceres-viewer PRIVATE $<$<COMPILE_LANGUAGE:CXX>:/W4> $<$<COMPILE_LANGUAGE:CUDA>:-allow-unsupported-compiler>)
    target_compile_definitions(ceres-viewer PRIVATE _ALLOW_COMPILER_AND_STL_VERSION_MISMATCH)
    target_link_libraries(ceres-viewer PRIVATE bcrypt crypt32 advapi32 shell32)
  endif()
  install(TARGETS ceres-viewer RUNTIME DESTINATION . COMPONENT ViewerRuntime)
  file(READ "${CMAKE_CURRENT_SOURCE_DIR}/assets/redistributable.json" ceres_assets_json)
  string(JSON ceres_asset_count LENGTH "${ceres_assets_json}" files)
  math(EXPR ceres_asset_last "${ceres_asset_count} - 1")
  foreach(index RANGE 0 ${ceres_asset_last})
    string(JSON asset GET "${ceres_assets_json}" files ${index})
    if(asset MATCHES "(^/|\\.\\.|[\\\\])" OR asset MATCHES "mano")
      message(FATAL_ERROR "Invalid redistributable asset: ${asset}")
    endif()
    get_filename_component(asset_directory "${asset}" DIRECTORY)
    install(FILES "assets/${asset}" DESTINATION "assets/${asset_directory}" COMPONENT ViewerRuntime)
  endforeach()
  install(FILES assets/redistributable.json DESTINATION assets COMPONENT ViewerRuntime)
  install(FILES README.md LICENCE.md DESTINATION . COMPONENT ViewerRuntime)
  install(DIRECTORY docs/ DESTINATION docs COMPONENT ViewerRuntime)
endif()
